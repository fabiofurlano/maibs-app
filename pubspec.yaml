name: maibs_app
description: MAIBS Companion — heart mode + head mode
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  audioplayers: ^6.1.0
  shared_preferences: ^2.3.0
  # liquid_glass_widgets: ^0.9.5  # REMOVED — broken
  # oc_liquid_glass: ^0.2.1       # REMOVED — broken

dependency_overrides:
  meta: ^1.17.0
  vector_math: ^2.3.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
